node_modules/
dist/
.DS_Store
*.log

# Pipeline working files. The CSV export is regenerated from Postgres and the
# gazetteer is re-downloaded; neither belongs in the repository.
pipeline/raw/
pipeline/*.log
pipeline/zcta.zip
pipeline/*_Gaz_zcta_national.txt

# Release candidates: a build's own copy of the export, the dataset, its logs
# and its release record. The promoted dataset lives in public/data; this is the
# working directory it was assembled in.
pipeline/out/

# The dataset a release replaced, kept until the site has been verified.
public/data.prev-*
